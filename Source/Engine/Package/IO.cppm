export module Package:IO;

// Utility for writing artifacts into package or file system
// Origin depends on build configuration
export class SPackageWriter {

};

// Utility for reading artifacts from package or file system
// Origin depends on build configuration
export class SPackageReader {

};